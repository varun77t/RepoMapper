# Baked into the bundle at build time by Vite, so it is a build input, not a
# runtime setting -- changing it needs a redeploy, not a restart.
#
# Not a secret: it is a public API base that ships inside the JavaScript
# every visitor downloads.
VITE_API_BASE=https://repomap-api.onrender.com
